# 파일

파일을 업로드하고 레코드에 첨부하거나, 레코드에 첨부된 파일을 조회·삭제합니다.

* 업로드 시 `objectType`·`objectId`를 함께 보내면 해당 레코드(딜·리드·고객·회사·상품·견적서·커스텀 오브젝트·노트)에 파일이 첨부됩니다.
* `objectType`·`objectId` 없이 업로드하면 파일만 업로드되며, 반환된 `id`를 이메일 발송(`POST /v2/email`)의 `attachmentIdList`에 사용할 수 있습니다.
